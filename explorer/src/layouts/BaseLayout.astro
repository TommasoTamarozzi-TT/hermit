---
import "../styles/global.css";
import HeaderNavLinks from "../components/HeaderNavLinks.astro";

interface CustomPageLink {
  href: string;
  label: string;
}

interface Props {
  title: string;
  roleId?: string;
  roleName?: string;
  customPageLinks?: CustomPageLink[];
}

const { title, roleId, roleName, customPageLinks = [] } = Astro.props;
const appName = "Raidyn Virtual Assistent - Explorer";
---
<!doctype html>
<html lang="en" class="h-full" data-theme="raidyn">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} · {appName}</title>
    <script is:inline>
      (() => {
        const storageKey = "hermit-explorer-theme";
        const defaultTheme = "raidyn";
        const allowedThemes = new Set(["light", "raidyn"]);
        try {
          const savedTheme = localStorage.getItem(storageKey) || defaultTheme;
          document.documentElement.dataset.theme = allowedThemes.has(savedTheme) ? savedTheme : defaultTheme;
        } catch {
          document.documentElement.dataset.theme = defaultTheme;
        }
      })();
    </script>
  </head>
  <body>
    <div class="app-shell">
      <header class="app-header">
        <div class="app-header-inner">
          <a href="/" class="brand-link" aria-label="Raidyn Virtual Assistent home">
            <span class="brand-text">RAIDYN VIRTUAL ASSISTENT</span>
            <img src="/raidyn-logo.png" alt="Raidyn" class="brand-logo" width="1000" height="240" decoding="async" />
          </a>

          <div class="header-actions">
            <label class="theme-switcher" for="theme-select">
              <span class="theme-switcher__label">Theme</span>
              <select id="theme-select" class="theme-select" data-theme-select>
                <option value="light">Light</option>
                <option value="raidyn">Raidyn</option>
              </select>
            </label>

            <details class="relative md:hidden">
              <summary class="mobile-menu-button">
                <span class="sr-only">Open menu</span>
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                  <path d="M4 6h16M4 12h16M4 18h16" stroke-linecap="round" />
                </svg>
              </summary>
              <div class="mobile-menu-panel">
                <nav class="flex flex-col gap-0.5">
                  <HeaderNavLinks roleId={roleId} roleName={roleName} customPageLinks={customPageLinks} mobile />
                </nav>
              </div>
            </details>

            <nav class="hidden items-center gap-2 md:flex" aria-label="Main">
              <HeaderNavLinks roleId={roleId} roleName={roleName} customPageLinks={customPageLinks} />
            </nav>
          </div>
        </div>
      </header>

      <main class="page-shell">
        <slot />
      </main>
    </div>

    <script is:inline>
      (() => {
        const storageKey = "hermit-explorer-theme";
        const defaultTheme = "raidyn";
        const select = document.querySelector("[data-theme-select]");
        if (!(select instanceof HTMLSelectElement)) {
          return;
        }

        const currentTheme = document.documentElement.dataset.theme || defaultTheme;
        select.value = currentTheme;

        select.addEventListener("change", () => {
          const nextTheme = select.value || defaultTheme;
          document.documentElement.dataset.theme = nextTheme;
          try {
            localStorage.setItem(storageKey, nextTheme);
          } catch {
            // Ignore storage failures and keep the current session theme.
          }
        });
      })();
    </script>

    <script is:inline>
      (() => {
        const closeExplorerFoldables = () => {
          document
            .querySelectorAll("details.foldable-card, details.procurement-section, details[data-home-todo]")
            .forEach((item) => {
              if (item instanceof HTMLDetailsElement) {
                item.open = false;
                item.removeAttribute("open");
              }
            });
        };

        closeExplorerFoldables();
        document.addEventListener("DOMContentLoaded", closeExplorerFoldables);
        document.addEventListener("astro:page-load", closeExplorerFoldables);
      })();
    </script>
  </body>
</html>
